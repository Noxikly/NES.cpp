#pragma once

#include <cstddef>
#include <cstdint>

using u8 = std::uint8_t;   /* 8-bit unsigned byte   */
using u16 = std::uint16_t; /* 16-bit unsigned short */
using u32 = std::uint32_t; /* 32-bit unsigned word  */
using u64 = std::uint64_t; /* 64-bit unsigned int   */

using i8 = std::int8_t;   /* 8-bit signed byte   */
using i16 = std::int16_t; /* 16-bit signed short */
using i32 = std::int32_t; /* 32-bit signed word  */
using i64 = std::int64_t; /* 64-bit signed int   */

using sz = std::size_t;

using f32 = float;
using f64 = double;
